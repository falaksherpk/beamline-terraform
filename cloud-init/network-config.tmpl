version: 2
ethernets:
  lab0:
    match:
      macaddress: "${mac_lab}"
    set-name: lab0
    addresses: [${ip}/24]
  nat0:
    match:
      macaddress: "${mac_nat}"
    set-name: nat0
    dhcp4: true
